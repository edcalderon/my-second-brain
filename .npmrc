# Enforce pnpm usage globally
engine-strict=true

# Use pnpm's lockfile v6+ format
lockfile-only=false

# Require exact version match (no caret or tilde)
exact-optional-peer-deps=true

# Prevent npm/yarn installation
prefer-frozen-lockfile=true

# Use workspace monorepo features
auto-install-peers=true
