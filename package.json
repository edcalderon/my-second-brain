{
    "name": "@ed/monorepo",
    "private": true,
    "description": "Monorepo for Edward's Second Brain and Knowledge Base",
    "scripts": {
        "dev": "pnpm --filter @ed/dashboard dev",
        "build": "pnpm --recursive build",
        "deploy:functions": "cd scripts && ./deploy-gcp.sh"
    },
    "devDependencies": {
        "pnpm": "latest"
    }
}